{
  "name": "elasticsearch-client",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/elasticsearch-client/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "tsc -p tsconfig.lib.json",
        "cwd": "libs/elasticsearch-client"
      }
    },
    "lint": {
      "executor": "@nx/eslint:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/elasticsearch-client/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/libs/elasticsearch-client"],
      "options": {
        "jestConfig": "libs/elasticsearch-client/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": ["shared", "lib"]
}
