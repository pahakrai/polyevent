{
  "name": "admin-service",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/nestjs-services/admin-service/src",
  "projectType": "application",
  "implicitDependencies": [
    "shared-types",
    "database-client",
    "auth",
    "utils"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": { "command": "nest build", "cwd": "apps/nestjs-services/admin-service" },
      "configurations": {
        "production": { "command": "nest build" },
        "development": { "command": "nest build" }
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "options": { "buildTarget": "admin-service:build", "port": 3009 },
      "configurations": {
        "production": { "buildTarget": "admin-service:build:production" },
        "development": { "buildTarget": "admin-service:build:development" },
        "debug": {
          "buildTarget": "admin-service:build:development",
          "inspect": true, "host": "0.0.0.0", "port": 9237, "watch": false
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:eslint",
      "outputs": ["{options.outputFile}"],
      "options": { "lintFilePatterns": ["apps/nestjs-services/admin-service/**/*.ts"] }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/apps/nestjs-services/admin-service"],
      "options": {
        "jestConfig": "apps/nestjs-services/admin-service/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": { "ci": { "ci": true, "codeCoverage": true } }
    },
    "container": {
      "executor": "@nx-tools/nx-container:build",
      "dependsOn": ["build"],
      "options": {
        "push": false, "engine": "docker",
        "metadata": {
          "images": ["polydom/admin-service"],
          "load": true,
          "tags": ["type=sha,prefix=sha-", "type=ref,event=branch"]
        }
      }
    }
  },
  "tags": ["service", "nest"]
}
