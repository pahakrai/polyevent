/**
 * Agent service has no seed data — investigation sessions are created at runtime.
 * This file exists to satisfy the npm run db:seed script contract.
 */
console.log('Agent service: no seed data required.');
process.exit(0);
