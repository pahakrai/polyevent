{
  "name": "agent-service",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/nestjs-services/agent-service/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "nest build --webpack",
        "cwd": "apps/nestjs-services/agent-service"
      },
      "configurations": {
        "production": {
          "command": "nest build --webpack"
        },
        "development": {
          "command": "nest build --webpack"
        }
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest start --webpack",
        "cwd": "apps/nestjs-services/agent-service"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest start --watch --webpack",
        "cwd": "apps/nestjs-services/agent-service"
      }
    },
    "lint": {
      "executor": "@nx/eslint:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/nestjs-services/agent-service/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/apps/nestjs-services/agent-service"],
      "options": {
        "jestConfig": "apps/nestjs-services/agent-service/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    },
    "db:generate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx drizzle-kit generate --config src/database/drizzle.config.ts",
        "cwd": "apps/nestjs-services/agent-service"
      }
    },
    "db:migrate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx drizzle-kit migrate --config src/database/drizzle.config.ts",
        "cwd": "apps/nestjs-services/agent-service"
      }
    },
    "db:push": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx drizzle-kit push --config src/database/drizzle.config.ts",
        "cwd": "apps/nestjs-services/agent-service"
      }
    },
    "db:seed": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsx src/database/seed.ts",
        "cwd": "apps/nestjs-services/agent-service"
      }
    },
    "container": {
      "executor": "@nx-tools/nx-container:build",
      "dependsOn": ["build"],
      "options": {
        "push": false,
        "engine": "docker",
        "metadata": {
          "images": ["polydom/agent-service"],
          "load": true,
          "tags": [
            "type=sha,prefix=sha-",
            "type=ref,event=branch"
          ]
        }
      }
    }
  },
  "tags": ["service", "nest"]
}
