apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

resources:
  - configmap.yaml
  - kafka-consumers.yaml
  - inference.yaml
  - ml-training.yaml

commonLabels:
  component: python-workers

images:
  - name: polydom/python-workers
    newTag: latest
