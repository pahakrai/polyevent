apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: production
spec:
  selector:
    app: api-gateway
    tier: backend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: ClusterIP