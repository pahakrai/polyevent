# Ensure at least 1 API Gateway replica is always available during voluntary disruptions.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: api-gateway-pdb
  namespace: production
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: api-gateway
